# ---------- Core ----------
NODE_ENV=development
PORT=4000
API_PREFIX=/api/v1
LOG_LEVEL=info
# summary = one line per request · off = errors only · full = everything
HTTP_LOG=summary
SLOW_QUERY_MS=250
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ---------- Database ----------
# Matches the salon-crm-postgres container:
#   docker run --name salon-crm-postgres \
#     -e POSTGRES_DB=salon_crm_db -e POSTGRES_USER=salon_app -e POSTGRES_PASSWORD=salon_password123 \
#     -p 5432:5432 -v salon_postgres_data:/var/lib/postgresql/data -d postgres
#
# The container creates salon_crm_db on first start, so there is nothing to create
# by hand — `prisma migrate dev` builds the 76 tables into it.
DATABASE_URL="postgresql://salon_app:salon_password123@localhost:5432/salon_crm_db?schema=public"

# ---------- Auth ----------
JWT_ACCESS_SECRET=change-me-access-secret
JWT_REFRESH_SECRET=change-me-refresh-secret
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=30d
BCRYPT_ROUNDS=10

# ---------- Platform ----------
PLATFORM_ADMIN_EMAIL=admin@salonos.in
PLATFORM_ADMIN_PASSWORD=Admin@12345

# ---------- Jobs ----------
JOB_WORKER_ENABLED=true
JOB_POLL_INTERVAL_MS=5000
JOB_BATCH_SIZE=25

# ---------- Messaging ----------
MESSAGING_DRIVER=console            # console | whatsapp_cloud | gupshup
WHATSAPP_API_URL=https://graph.facebook.com/v20.0
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_WEBHOOK_VERIFY_TOKEN=
# Platform fallbacks only — each salon connects its own accounts in the app.
SMS_DRIVER=console            # console | msg91
SMS_API_KEY=
SMS_SENDER_ID=
SMS_DLT_ENTITY_ID=
# Email via Resend (https://resend.com). Paste the key and a from-address on a
# domain you have verified there; the driver switches on by itself. Use
# onboarding@resend.dev only for testing — it can only mail your own inbox.
RESEND_API_KEY=
RESEND_FROM_EMAIL=
RESEND_FROM_NAME=
# The generic names still work if you prefer them:
EMAIL_DRIVER=console          # console | resend (auto when RESEND_API_KEY is set)
EMAIL_API_KEY=
EMAIL_FROM_ADDRESS=
EMAIL_FROM_NAME=

# ---------- Business defaults ----------
DEFAULT_CURRENCY=INR
DEFAULT_TIMEZONE=Asia/Kolkata
DEFAULT_GST_RATE=18

#docker run --name salon-crm-postgres -e POSTGRES_DB=salon_crm_db -e POSTGRES_USER=salon_app -e POSTGRES_PASSWORD=salon_password123 -p 5432:5432 -v salon_postgres_data:/var/lib/postgresql/data -d a426e44bac0b
# # Role	Email
# Owner	owner@glowstudio.in
# Manager	manager@glowstudio.in
# Receptionist	reception@glowstudio.in
# Accountant	accounts@glowstudio.in
# Salon@12345
